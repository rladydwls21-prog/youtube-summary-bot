# 이 파일은 템플릿입니다. 실제 키는 `.env` 파일에 따로 저장하세요.
# `.env`는 .gitignore에 포함되어 있어 커밋되지 않습니다.
#
# 사용법:
#   1. 이 파일을 복사해서 `.env`로 이름 바꾸기
#   2. 아래 변수에 실제 값 채우기
#   3. Tool은 `.env`에서만 키를 읽습니다 — 코드에 직접 박지 마세요
#
# 키는 실제 Workflow/Tool이 추가될 때 같이 채워집니다.
# 지금은 어떤 외부 서비스를 쓸지 정해지지 않았으므로 비어 있습니다.
